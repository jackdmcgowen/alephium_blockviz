// blockviz_engine is a Vulkan-free product facade.
// Concrete Vulkan implementation (VulkanEngine, frame/*, sobel, pipelines) and
// create_blockviz_engine() live in graphics.lib. This TU keeps the project valid.
