#pragma once

// DEPRECATED — fullscreen lives in app/platform/app_platform_win32.cpp.
// Kept as an empty include guard so old references fail loudly if reintroduced.
// See docs/platform.md.

#error "window_fullscreen.hpp removed; use app/platform/app_platform.hpp"
