# VnV · mod (module tests)

Non-graphics, CPU-only verification.

| Project | Sources | Binary |
|---------|---------|--------|
| `mod_domain` | `vnv/mod/tests/domain_tests.cpp` | `build/mod_domain/<Config>/mod_domain.exe` |

```powershell
.\scripts\run_vnv.ps1          # default runs mod
.\scripts\run_vnv.ps1 -Mod
```
